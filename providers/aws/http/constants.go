package http

const searchURL = "https://aws.amazon.com/api/dirs/items/search"

const (
	directoryIDBlogPosts = "blog-posts"
	sortByCreatedDate    = "item.additionalFields.createdDate"
	sortOrderDesc        = "desc"
	entryLimit           = 50
)

const categoryTagPrefix = "blog-posts#category#"
